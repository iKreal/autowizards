import { createRouteHandlerSupabase, getRouteHandlerSession } from "./route-handler";

export const createServerActionSupabase = createRouteHandlerSupabase;

export const getServerActionSession = getRouteHandlerSession;
